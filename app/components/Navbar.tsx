import React, {ReactNode} from 'react';
import { Text, View } from 'react-native';
import { Link } from "expo-router";

const Navbar = () => {
    return(
        <View className = 'flex w-full h-full bg-ug-gray'>
            <Text>test</Text>
        </View>
    )
}

export default Navbar